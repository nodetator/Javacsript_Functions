function add7(x) {
    x += 7;
    return x;
}

function multiply(a, b) {
    return a * b; 
}

function capitalize (x) {
    y = x.slice(1);
    x =  x.charAt(0);
    x = x.toUpperCase();
    return x + y;
}

function lastLetter (x) {
    x = x.slice(-1);
    return x;
}